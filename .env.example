# Supabase (obligatoire pour le CRM - liste clients, etc.)
# Sur Render : utiliser ces noms exacts
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # Clé "service_role" dans Supabase Dashboard > Settings > API
# Optionnel (fallback si pas de service_role) :
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# Firebase (pour auth admin, conseillers)
# ...

# Autres (PayMe, etc.)
# ...
